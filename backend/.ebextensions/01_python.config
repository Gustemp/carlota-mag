option_settings:
  aws:elasticbeanstalk:application:environment:
    PORT: "8000"
  aws:elasticbeanstalk:environment:proxy:staticfiles:
    /uploads: uploads

container_commands:
  01_mkdir:
    command: "mkdir -p uploads/pdfs uploads/covers"
